import * as React from "react";

import * as A from "antwerp-core-react-branding";

const Hero = () => (
    <A.Hero title="Antwerp Core React Branding" subtitle="Overview of the styles" color="#f1f1f1">
    </A.Hero>
);

export default Hero;
